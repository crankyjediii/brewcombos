# Optional for local catalog, builder, and device saves. Required for AI mixing.
OPENROUTER_API_KEY=
OPENROUTER_MODEL=
OPENROUTER_FALLBACK_MODELS=
RATE_LIMIT_PER_MIN=6
SITE_URL=https://brewcombos.com

# Required for email accounts, cloud saves, and reviewed community submissions.
# Apply supabase/migrations/202609250001_community.sql first.
# Use a public anon/publishable key here, NEVER a service-role/secret key.
SUPABASE_URL=
SUPABASE_ANON_KEY=
